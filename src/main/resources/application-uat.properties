server.port=8080

# Eureka
eureka.client.service-url.defaultZone=http://uat-registry.yourdomain.com:8761/eureka/

# JWT — must match the secret in the auth service
jwt.secret=${JWT_SECRET}

# Routes are defined in GatewayConfig.java
